import { Application, System } from '../engine/framework'
import { IBehaviour } from '../engine/Animation'

export class OpponentSystem implements System {
    constructor(private readonly context: Application){}
    update(){

    }
}